# Local Claude Code / per-user settings (not shared)
.claude/settings.local.json

# Superpowers SDD scratch
.superpowers/

# Implementation plans kept local (may name concrete tools / local paths)
docs/superpowers/plans/

# Real local config + captured env-specific artifacts (never committed)
nelix.toml
tests/fixtures/
spikes/frames/
FEASIBILITY.md
docs/HANDOFF.md

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# OS / editor cruft
.DS_Store
*.swp
