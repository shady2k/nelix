# Local Claude Code / per-user settings (not shared)
.claude/settings.local.json

# Superpowers SDD scratch
.superpowers/

# Implementation plans + design specs kept local (may name concrete tools / local paths)
docs/superpowers/plans/
docs/superpowers/specs/

# Real local config + captured env-specific artifacts (never committed)
nelix.toml
tests/fixtures/
spikes/frames/
FEASIBILITY.md
docs/HANDOFF.md

# Python
__pycache__/
*.py[cod]
.venv
venv/
*.egg-info/
build/
dist/

# OS / editor cruft
.DS_Store
*.swp
spikes/_spike_d_raw
spikes/_spike_d_transcript.txt

# VT-render spike build artifacts (downloaded Zig + ghostty source + built wasm)
spikes/vt-ghostty/.build/
